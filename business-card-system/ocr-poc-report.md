# OCR精度 PoC 報告

**[open-issues-v0.3.md 論点C](./open-issues-v0.3.md#論点cocrサービスの選定) に対する第1回計測**

---

## 1. この報告の位置づけ

論点Cは「OCRサービスをどう選ぶか」だった。今回のPoCで確認したのは次の2点。

1. **どの構成が精度・時間・費用で有利か**を、推測ではなく**実測**で比較できる仕組みを作ること
2. その仕組みで現行実装を測り、**改善できる箇所を特定して直すこと**

計測は再現可能なコードとして `app/poc/` に置いてある。実名刺が用意できた段階で、
同じコマンドに `--real` を付けるだけで本番相当のデータで測り直せる。

```bash
cd app
./.venv/bin/python -m poc.runner --out ../ocr-poc-report.md          # 合成サンプル
./.venv/bin/python -m poc.runner --real ./poc/samples --out ../x.md  # 実名刺
```

> **重要な前提**：今回のサンプルは**合成画像**である。実名刺のフォント・紙質・印刷のかすれ・
> 逆光などは再現できていないため、**絶対値は実データでの精度を保証しない**。
> 構成間の相対比較と、改善の効果測定に使うことを目的としている。

---

## 2. 結論

| # | 結論 | 根拠 |
| --- | --- | --- |
| 1 | **表示用の画像補正をOCRに掛けてはいけない** | 補正あり 54.8% → 補正を外して 68.0%（**+13.2ポイント**） |
| 2 | 傾き補正は**1.5度未満では掛けない**ほうがよい | 微小回転の再サンプリングで文字が甘くなり、-5ポイント |
| 3 | 縦長の名刺を横長に自動回転してはいけない | 縦書き名刺のOCRを壊す。回転をやめて縦書き言語データを併用 |
| 4 | 縦書き名刺は**依然として最も苦手**（修正10項目/枚） | tesseractの限界。クラウドOCRまたはLLM抽出で再評価が必要 |
| 5 | ふりがなが**ほぼ0%だったのは分割規則の誤りで、OCRの限界ではなかった** | せい0%・めい6% → **どちらも50%**（[4.10](#410-ふりがなを先頭の空白で切っていた)）。「OCRが弱い」と読める数字が、実は自分たちの取り出し方だったという例 |

**実装済みの改善**：結論1〜3はアプリ本体に反映済み。
表示用画像とOCR用画像を分離し（`services/images.py`）、原本として保存するのは補正前の画像に変更した。
これにより再処理時のOCR精度も落ちない。

**未確定のまま残ること**：方式2（LLM抽出）とクラウドOCRは、この環境に認証情報が無いため未計測。
アダプタの実装と計測の受け皿は用意してあるので、鍵を設定すれば同じ手順で数値が出る。

---

## 3. 計測結果

<!-- 計測結果ここから（poc.runner が自動で書き換えます。手で編集しないこと） -->

### 計測条件

- サンプル枚数：20 枚（レイアウト4種 × 撮影条件2種 × 人物違い）
- 一致判定：全角半角・空白・記号を正規化したうえでの完全一致（電話番号は数字のみで比較）
- 修正操作回数：正解と一致しなかった項目数（未取得・誤り・余分な出力の合計）
- 構成E（LLM抽出）は Claude API の認証情報が無い環境のため未計測。ANTHROPIC_API_KEY を設定して同じコマンドを実行すると計測される

### 構成別の結果

| 構成 | 項目正答率 | 1枚あたり修正項目数 | 処理時間/枚 | 費用/枚 |
| --- | ---: | ---: | ---: | ---: |
| A 前処理なし | 65.6% | 4.5 項目 | 2.25 秒 | — |
| B 改善前の前処理 | 54.8% | 5.8 項目 | 2.78 秒 | — |
| C 改善後(横書き言語) | 68.0% | 4.2 項目 | 3.16 秒 | — |
| D 改善後(縦書き言語追加) | 68.0% | 4.2 項目 | 4.68 秒 | — |
| E 改善後+LLM抽出 | 未計測 | 未計測 | 未計測 | 未計測 |
| F 改善後+PaddleOCR | 未計測 | 未計測 | 未計測 | 未計測 |
| G 改善後+EasyOCR | 64.1% | 4.7 項目 | 16.80 秒 | — |

### 項目別の正答率

| 項目 | A 前処理なし | B 改善前の前処理 | C 改善後(横書き言語) | D 改善後(縦書き言語追加) | E 改善後+LLM抽出 | F 改善後+PaddleOCR | G 改善後+EasyOCR |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 姓 | 65% | 55% | 60% | 60% | — | — | 60% |
| 名 | 60% | 50% | 55% | 55% | — | — | 60% |
| せい | 65% | 55% | 65% | 65% | — | — | 80% |
| めい | 65% | 60% | 65% | 65% | — | — | 80% |
| 会社名 | 60% | 55% | 70% | 70% | — | — | 75% |
| 部署 | 75% | 55% | 65% | 65% | — | — | 50% |
| 役職 | 70% | 55% | 65% | 65% | — | — | 65% |
| 郵便番号 | 80% | 60% | 75% | 75% | — | — | 90% |
| 住所 | 70% | 50% | 90% | 90% | — | — | 65% |
| 電話 | 75% | 70% | 85% | 85% | — | — | 95% |
| 携帯 | 50% | 50% | 57% | 57% | — | — | 79% |
| FAX | 75% | 42% | 75% | 75% | — | — | 50% |
| メール | 60% | 55% | 70% | 70% | — | — | 20% |
| URL | 38% | 46% | 46% | 46% | — | — | 8% |

### レイアウト・撮影条件別の修正項目数（1枚あたり）

| 条件 | A 前処理なし | B 改善前の前処理 | C 改善後(横書き言語) | D 改善後(縦書き言語追加) | E 改善後+LLM抽出 | F 改善後+PaddleOCR | G 改善後+EasyOCR |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| bilingual/photo | 8.0 | 6.0 | 5.0 | 5.0 | — | — | 6.5 |
| bilingual/scan | 1.0 | 3.0 | 1.0 | 1.0 | — | — | 1.0 |
| decorated/photo | 5.0 | 2.5 | 2.5 | 2.5 | — | — | 4.5 |
| decorated/scan | 2.5 | 2.5 | 2.5 | 2.5 | — | — | 2.5 |
| spread/photo | 3.0 | 4.5 | 2.5 | 2.5 | — | — | 7.0 |
| spread/scan | 2.5 | 3.0 | 2.5 | 2.5 | — | — | 0.5 |
| standard/photo | 2.0 | 7.0 | 4.5 | 4.5 | — | — | 2.0 |
| standard/scan | 1.0 | 3.5 | 1.0 | 1.0 | — | — | 1.5 |
| vertical/photo | 10.0 | 13.5 | 10.5 | 10.5 | — | — | 12.5 |
| vertical/scan | 9.5 | 13.0 | 9.5 | 9.5 | — | — | 8.5 |

### 実行できなかった構成

- **E 改善後+LLM抽出**：Claude API の認証情報が設定されていません。
- **F 改善後+PaddleOCR**：No available model hosting platforms detected. Please check your network connection.

<!-- 計測結果ここまで -->

---

## 4. 何が起きていたか（改善の内訳）

### 4.1 表示用の補正がOCRを壊していた

改善前は、1枚の補正済み画像を「表示にもOCRにも」使っていた。工程ごとに切り分けて測ると：

| 工程 | 項目正答率 |
| --- | ---: |
| 生ページ | 43.4% |
| ＋名刺の切り出し（台形補正） | 43.4% |
| ＋傾き補正（0.3度以上で回転） | 38.1% |
| ＋明るさ・影の補正（CLAHE＋影除去） | 34.0% |

人が見やすくするための補正（コントラスト強調・影の除去）は、OCRにとっては文字の輪郭を
なまらせる処理でしかなかった。**表示用とOCR用を分ければ、どちらも犠牲にしない。**

### 4.2 効果がなかった／悪化した施策

「やってみたが採用しなかった」ものも記録しておく。同じ検討を繰り返さないため。

| 施策 | 結果 | 判断 |
| --- | ---: | --- |
| 二値化（大津の方法） | 25.8% | 不採用（大幅悪化） |
| 適応的二値化 | 32.1% | 不採用 |
| アンシャープマスク | 36.3% | 不採用 |
| 高解像度化（長辺2200px）＋シャープ化 | 30.8% | 不採用（悪化かつ処理時間2倍） |
| 弱いコントラスト補正のみ | 36.3% | 不採用 |
| 縦長画像の向き総当たり（0/90/270度でOCRし最良を採用） | 変化なし | 不採用（時間だけ増える） |
| 向き検出(OSD)による90度単位の回転補正 | 回転した名刺で 2項目 → 14項目 | **採用**（下記4.3） |
| ページ分割モード psm 5 の追加 | 変化なし | 不採用 |
| 縦書き言語データ `jpn_vert` の併用 | +0.5ポイント | **採用**（処理時間は +1.1秒/枚） |


### 4.3 90度回った名刺は、OCRがほぼ何も読めない

実テストで「名刺として正しく仕分けられているのに、項目が1つも取れない」という報告があった。
原本を見ると、読み取り機に横向きに置いたために画像ごと90度回っていた。
人が見れば読めるので「OCRが弱い」と誤解されやすいが、原因は別物だった。

| 入力 | 取れた項目 |
| --- | ---: |
| 正立 | 14 / 14 |
| 90度回転 | 2 / 14 |

傾き補正（`deskew`）が扱うのは数度のズレで、90度は対象外だった。
tesseract の向き検出（OSD）で90度単位の回転だけを別に直すようにした（`services/orientation.py`）。
補正後は 0/90/180/270度のいずれで入れても 14/14 に戻る。

**縦書き名刺を巻き添えにしないこと**が最大の懸念だった（形だけで回すと壊れるのは4.2で不採用にしたとおり）。
OSDは字の形から向きを読むため縦書きも正立と判定し、PoCサンプル16枚（縦書きのscan/photoを含む）は
**全て回転0度**、つまり1枚も誤って回さないことを確認したうえで採用した。
確信度が低い判定は採用しない（誤って回す害のほうが大きいため）。


### 4.4 「OCRは読めているのに、取り出し方で落としていた」3件

項目ごとの正答率を1枚ずつ突き合わせたところ、OCRの限界ではなく
**こちらの取り出し方の問題**で落としていたものが3件あった。

| 項目 | OCRが読んだ文字 | 取れていた値 | 原因 |
| --- | --- | --- | --- |
| URL | `https://www. example. co. jp` | `https://www.` | ドットの後の空白で正規表現が止まっていた（16枚中4枚） |
| メール | `taro, yamada@example.co.jp` | `yamada@example.co.jp` | ローカル部のドットがカンマと読まれ、先頭が丸ごと落ちていた |
| 住所 | `7100-0001 東京都…` | `7 東京都…` | 〒 が `7` と読まれ、郵便番号を抜いた残りに付いてきていた |

いずれも直した。**総合 45.2% → 51.0%**（1枚あたりの修正項目数 7.2 → 6.4）。

| 項目 | 修正前 | 修正後 |
| --- | ---: | ---: |
| 住所 | 38% | **69%** |
| URL | 17% | **50%** |
| メール | 44% | **62%** |

直しすぎると別のものを壊すため、次の3つは壊れないことを確認したうえで入れている。

- `info@a.co.jp, sales@a.co.jp` のようにメールを2つ並べた行で、前のアドレスを巻き込まない
- `7-1-1 Chiyoda, Tokyo 100-0001` のような英字表記で、番地の `7` を消さない
- `https://tech.example.jp Mobile 090-…` で、URLに電話番号を繋げない


### 4.5 1行に並んだ部署と役職を分けていなかった

名刺は「開発部 主任」のように部署と役職を1行に印字することが多い。
部署がその行を丸ごと取っていたため、**1行で2項目とも落としていた**。

| OCRが読んだ行 | 部署（正解） | 部署（取得） | 役職（取得） |
| --- | --- | --- | --- |
| `開発部 主任` | `開発部` | `開発部主任` | `開発部主任` |
| `営業本部 第一営業部 部長` | `営業本部 第一営業部` | `営業本部第一営業部部長` | `部長` |

役職の語が行末側にあり、その手前に部署の語がある場合に分けるようにした。
**総合 51.0% → 52.9%**（部署 50% → 62%、役職 50% → 62%）。

切りすぎると別の壊れ方をするため、次を確認したうえで入れている。

- `本部長` は `本部` を含むが部署ではない（行全体が役職の語と一致する場合は部署にしない）
- `Sales Department` の `Sales` を役職にしない（役職の語が行頭にある場合は分けない）
- `シニアエンジニア` を `エンジニア` に切らない（手前に部署の語が無い場合は分けない）


### 4.6 海外名刺の電話番号を1件も拾えていなかった

海外名刺が多いという前提を確認する過程で見つけた。国番号つきの番号を
**`+81` だけ**見ていたため、それ以外の国の電話番号を拾えていなかった。

| 名刺 | 電話 |
| --- | --- |
| `TEL +1 212-555-0100`（米国） | 空 |
| `TEL +7 495 123-45-67`（ロシア） | 空 |
| `Tel: +44 (0)20 7123 4567`（英国） | 空 |

英語の名刺は他の6項目が取れていたため、**電話だけが欠ける**形で表面化していた。
国番号のあとを「数字と区切りの並び」として扱うようにし、上記すべてを拾えるようにした
（桁数は E.164 の15桁まで）。日本語名刺の精度は 52.9% で変化なし。

**あわせて重複人物の判定も直した。** `+81 90-1234-5678` と `090-1234-5678` は
同じ番号だが別の値になっていたため、同じ人が2枚の名刺で別人として登録されていた。
正規形を国内表記に揃え、既存データは移行で詰め替える（`a4f18b2c7e05`）。
国際表記の `(0)`（国内でのみ0を付ける慣習表記）も揃えている。

他国の携帯は見分け方が国ごとに違うため、国番号を残したまま「電話」に入れる。
誤って「携帯」に入れるより無害という判断。


### 4.7 外国名・英字表記の名刺で、6項目を取りこぼしていた

実データ（チリの制作会社の名刺）で、記載のある7項目のうち3項目しか
取れていなかった。**OCRは読めていて、抽出側で落としていた。**

| 項目 | 修正前 | 修正後 | 原因 |
| --- | --- | --- | --- |
| 姓・名 | `AONE` / `GAMES` | `パトリシオ` / `バスケス` | ロゴの英字を氏名にしていた |
| ふりがな | `パトリシオ` / `バスケス` | 空 | カタカナの氏名をふりがな扱いにし、姓名が空になっていた |
| 会社名 | 空 | `AONE GAMES` | 法人格の語（`株式会社`・`Inc.`）を含まないため拾えなかった |
| 役職 | `プロデューサー` | `エグゼクティブ・プロデューサー` | 和英併記の行から語だけを取り、修飾を捨てていた |
| 住所 | 空 | `Santa Beatriz 111 of 1008, …` | 都道府県の語が無いため拾えなかった |

直した規則は次の5つ。

1. **ふりがなはひらがなに限る。** カタカナだけの行は氏名として扱う
   （外国名の名刺はカタカナで氏名を印字する）
2. **全部大文字の英字は氏名にしない。** ロゴや社名の綴り。人名は通常そう書かない
3. **法人格の語が無い社名は、メールのドメインとの一致で見つける**
   （`AONE GAMES` ↔ `patricio@aonegames.com`）。一致しなければ空のままにする
4. **役職は和英併記の区切り（`/`）で日本語側を採り**、修飾を含む全体を残す
5. **住所が他から取れないときだけ**、数字を含む複数語の最長行を住所とする
   （メール・URL・語数の少ない行は除く）

カタカナ名の分け方も直した。OCRが語の途中に空白を入れるため
（`パトリシオ　バスケス` → `パト リシオ バスケス`）、先頭の空白で切って
`パト` / `リシオ バスケス` になっていた。どこが語の切れ目かは字面では
決まらないので、**長さの釣り合いがいちばん良い位置**で分ける。
日本語・英字の氏名の分け方は変えていない。

日本語の名刺への影響はなく、合成サンプルの総合は 52.9% で変わらない
（この名刺の型が合成サンプルに無いため、数値には表れない）。

### 4.8 電話番号を郵便番号として登録していた

実データ（韓国の方の名刺）で、記載のある8項目のうち4項目が空、
うち2項目に**誤った値**が入っていた。空欄より悪い。

| 項目 | 修正前 | 修正後 | 原因 |
| --- | --- | --- | --- |
| 郵便番号 | `070-9385` | `580-0021` | 携帯番号 `070-9385-4004` の前半を郵便番号として取っていた |
| 住所 | `-4004` | `大阪府松原市高見の里六丁目7-18` | 上の残り。本来の住所行は `Add` のラベルごと未使用のまま捨てていた |
| 電話 | 空 | `050-3110-2873` | `Tel 050-…` を携帯として扱い、先に入っていた携帯に押し出されて消えた |
| 携帯 | `070-9385-4004` | 同じ | `HP` を携帯のラベルとして知らず、先頭3桁だけで判定していた |

直した規則は次の4つ。

1. **郵便番号は前後を見る。** 後ろに数字や区切りが続くならその番号の途中
   （`070-9385`**-4004**）。前に数字が接している場合も同様。ただし
   〒 の読み違え（`7100-0001`）は例外として通す
2. **`HP`（handphone）を携帯のラベルに加える。** 韓国・台湾・東南アジアの
   名刺で使われる。英字のラベルは語の先頭に限る（`graphpad` の `hp` に
   当たらないように。`telephone` の `tel` は従来どおり拾う）
3. **ラベルが直に付いている番号は、ラベルを信じる。** `050` はIP電話で
   携帯ではないため先頭3桁の一覧から外し、`Tel 050-…` は電話として扱う。
   先頭3桁での見直しは、ラベルと番号の間に別の番号が挟まる場合
   （`TEL 03-… / 090-…`）に限る
4. **住所のラベル（`Add` `Address` `住所` `所在地`）を値に含めない。**
   郵便番号も都道府県も無い住所は、このラベルを頼りに拾う
   （英字の語は後ろに文字が続くものを除く。`Addison Road` を
   `ison Road` にしないため）

ラベルの照合を見直したついでに、隣接する2つの取りこぼしも直した。

- **`代表取締役` の `代表` を電話のラベルと数えていた。** 代表電話のラベル
  `代表` は残したまま、役職の一部（取締役・理事・社員が続く形）のときだけ
  数えないようにした
- **役職と番号を1行に印字した名刺**（`代表取締役 090-1234-5678`）で、
  番号を採った時点で行ごと使用済みにし、役職が空になっていた。番号と
  ラベルを外した残りから役職を探す

合成サンプルでも、郵便番号の判定を厳しくした副作用として住所が2枚改善した。
桁が合わず郵便番号として取れなかった `〒…` の残骸（`〒5390-0091 大阪府…`）を
住所から外し、読み崩れたURLの行（`tal 7 <kaido-foods.example`）を住所に
入れないようにしたため。

総合 52.9% → **53.8%**、修正項目 6.19 → **6.06 項目/枚**、住所 69% → **81%**。
なお郵便番号が読めなかった枚では、以前は誤った番号（FAX番号の一部）が
入っていたのに対し、いまは空欄になる。数値上はどちらも不正解だが、
**空欄のほうが入力する人には直しやすい**という判断。

### 4.9 海外の法人格を知らないため、社名が空になっていた

5.1 の計測で分かっていた「言語データを入れても項目が埋まらない」の本体。
法人格の辞書が `株式会社` と `Inc.` しか持たず、次のような社名を1件も
拾えていなかった。

| 名刺 | 修正前 | 修正後 |
| --- | --- | --- |
| `주식회사 오모로봇`（韓国） | 空 | `주식회사 오모로봇` |
| `欧姆机器人有限公司`（中国） | 空 | `欧姆机器人有限公司` |
| `ООО «Ромашка»`（ロシア） | 空 | `ООО «Ромашка»` |
| `Omorobot GmbH`（ドイツ） | 空 | `Omorobot GmbH` |

`주식회사` `(주)` `㈜` `유한회사` `有限公司` `股份公司` `集团` `有限責任公司`
`ООО` `ЗАО` `ОАО` `ПАО` `GmbH` `S.A.` `SARL` `B.V.` `N.V.` `Sdn Bhd` `PLC` `LLP`
を辞書に加えた。**`AG`・`SA`・`AB` のような2文字の略号は入れていない**
（`MANAGER` の中の `AG` のように別の語の一部に当たるため）。

3文字以上でも同じ問題は起きる。`S.A.` は住所の `U.S.A.` に当たり、
**住所の行が社名として登録されていた**（`123 Main St, Chicago, U.S.A.`）。
英字の略号は、直前が英字またはドットのときは数えないようにした
（`Acme S.A.` は数える。`U.S.A.` は数えない）。

**あわせて空白の扱いを文字体系ごとに分けた。** OCRの日本語出力は語の途中に
空白を入れるので消していたが、その規則を「ASCII以外の文字どうしの間」に
掛けていたため、ハングルやキリル文字の**語の区切り**まで消していた。

| 印字 | 修正前 | 修正後 |
| --- | --- | --- |
| `株式会社 サンプル 商事` | `株式会社サンプル商事` | 同じ（変えない） |
| `주식회사 오모로봇` | `주식회사오모로봇` | `주식회사 오모로봇` |
| `ООО «Ромашка»` | `ООО«Ромашка»` | `ООО «Ромашка»` |

空白を消す対象を、かな・漢字・全角記号に限った。日本語・中国語は語を空白で
区切らないが、ハングル・キリル文字・英字は区切るため。

法人格を外した照合値（`normalize_company`）も揃えているので、`주식회사 오모로봇`
と `오모로봇` は同じ会社として扱われる（重複人物・重複会社の判定に使う）。


### 4.10 ふりがなを先頭の空白で切っていた

**この報告で「OCRの限界」と書いていた数字が、実は自分たちの誤りだった。**

ふりがなの正答率は せい0%・めい6% で、結論5に「ルールベースではほぼ0%」と
書いていた。原因を追うと、OCRは読めていた。

| 印字 | OCRの読み | 修正前 | 修正後 |
| --- | --- | --- | --- |
| `やまだ たろう` | `や まだ た ろう` | `や` / `まだ た ろう` | `やまだ` / `たろう` |
| `すずき いちろう` | `すず き いち ろう` | `すず` / `き いち ろう` | `すずき` / `いちろう` |
| `さとう はなこ` | `さと う は な こ` | `さと` / `う は な こ` | `さとう` / `はなこ` |

かなは字の間隔が広いため、OCRが語の途中にも空白を入れる。**先頭の空白で
切っていた**ので、1文字目だけが「せい」になっていた。どこが語の切れ目かは
字面では決まらないので、**長さの釣り合いがいちばん良い位置**で分ける
（姓と名は極端に長さが違わない）。外国名のカタカナ氏名で先に入れた規則
（[4.7](#47-外国名英字表記の名刺で6項目を取りこぼしていた)）を、
ひらがなにも広げたもの。

**あわせて `Head Office` を氏名として登録していた。** 和英併記の名刺で、
姓 `Head` / 名 `Office` が入り、本来の氏名が空になっていた（16枚中4枚）。
英字2〜3語の行を人名と見なす判定に、場所を表す語（`Office` `Building`
`Branch` `Floor` など）の除外が無かった。

| 項目 | 修正前 | 修正後 |
| --- | ---: | ---: |
| せい | 0% | **50%** |
| めい | 6% | **50%** |
| 姓 | 38% | **56%** |
| 名 | 38% | **56%** |

総合 53.8% → **63.8%**、修正項目 6.06 → **4.75 項目/枚**。
16枚で22項目が改善し、1項目が悪化した（`すず` が読み落とされた1枚で、
釣り合いの位置がずれた）。

> **残るふりがなの誤りは、分割では救えない。** `すず き いち ろう` の
> `すず` のように**OCRが文字を落としている**もので、読み取りの精度そのものが
> 要る。LLM抽出でも埋まらない（入力に文字が無い）。ここはクラウドOCRや
> PaddleOCR の比較で見る領域。


### 4.11 役職と氏名が1行に読まれると、両方とも失う

実データ（韓国の方の名刺）で、郵便番号・住所・電話を直したあとも
**姓・名だけが空**のままだった。原因は行の読まれ方だった。

| | 役職 | 姓・名 |
| --- | --- | --- |
| OCRの読み | `代表取締役 ユン ソクン` | — |
| 修正前 | `代表取締役ユンソクン` | 空 |
| 修正後 | `代表取締役` | `ユン` / `ソクン` |

役職の語のあとに氏名らしい文字が続く場合、役職はその語だけにして、
残りを氏名に回すようにした。`部長 山田 太郎`・`CEO John Smith` も同様に分かれる。

**役職の続きと氏名の区別**が要る。`主任研究員` を役職『主任』＋氏名『研究員』に
分けてしまい、計測が 63.8% → 63.3% に落ちた（合成サンプルで検知）。
役職の語尾（`員` `長` `補佐` `代理` `職` など）で終わるものは氏名にしない。

#### 追って見つかった、氏名の判定そのものの取りこぼし2件

調べる過程で、**この名刺とは関係なく**氏名を落としていた条件が2つ見つかった。
どちらも珍しい姓ではない。

| 例 | 症状 | 原因 |
| --- | --- | --- |
| `佐々木 健` | 氏名として認識されない | 文字クラスに踊り字 `々` が無かった |
| `中村 健` | 同上 | 住所の語（`村`）を1つ含むだけで住所とみなしていた。`木村` `村上` `市川` `町田` も同じ |

住所は語を複数含むので、**2つ以上のときだけ**住所とみなすように変えた
（番地のある行は数字の判定で先に弾いている）。

あわせて `課長補佐` が**部署**として登録され役職が空になっていた不具合も直した
（`課` を部署の語として拾っていた）。役職の語で始まる行は、後ろに部署の語が
無ければ役職として扱う。`Sales Department` は従来どおり部署のまま。

> **合成サンプルの数値は 63.8% で変わらない。** 16枚の中に、役職と氏名を1行に
> 印字した名刺も、`佐々木`・`中村` のような姓も無いため。**合成サンプルで
> 測れるのは、そこに含まれる型だけ**だという例として記録しておく。


### 4.12 姓と名を離して印字した名刺で、氏名が総崩れになる

実データ（同じ組織の名刺2枚）。姓と名を大きく離して印字する体裁で、
離れたぶんが**別々の行**として読まれていた。

```
印字   とみた　　おさむ        OCRの読み   とみた
       冨田　　　修                        おさむ
                                           冨田
                                           修
```

| 項目 | 修正前 | 修正後 |
| --- | --- | --- |
| 姓・名 | `お` / `さむ` | `冨田` / `修` |
| せい・めい | `と` / `みた` | `とみた` / `おさむ` |

「ひらがなの行の次が氏名」という規則が、ふりがなの後半（`おさむ`）を氏名と
判断していた。**漢字の氏名は使われないまま捨てられていた。** 直した規則は3つ。

1. ひらがなの行の**直後もひらがなだけなら、それはふりがなの続き**（氏名ではない）
2. 氏名の行の次が短い漢字だけの行なら、それは**名**（`冨田` / `修`）。
   1文字の名（`修` `健`）があるため字数ではなく字種で見る。ただし**3文字まで**
   （4文字だと氏名がまるごと入った行を名にしてしまう。実測で発生）
3. **ふりがなは漢字より必ず長い**（`山田太郎` → `やまだたろう`）。氏名の行と
   同じか短いひらがなは、ふりがなではなく**かなの名**（`伊藤` + `しの`）

あわせて `事務所` `役所` `県庁` `市役所` `商工会議所` `組合` を法人格の語に加えた。
実データの `沖縄県 東京事務所` が、法人格の語を含まずメールのドメイン
（`pref.okinawa.lg.jp`）とも一致しないため空になっていた。`法律事務所`・
`設計事務所` にも効く。

> **合成サンプルの数値は 63.8% で変わらない**（16枚にこの体裁が無いため）。
> 実データでしか見つからない型であり、**合成サンプルだけで精度を語れない**
> ことの2つ目の例。

#### 付随して見つかった、道具側の不具合

`この1枚を調べる`（`windows/explain-one.bat`）が、**名刺のファイルを指定すると
コマンドプロンプトごと落ちていた**。ファイル名の丸括弧（`(会社名)_冨田 修.pdf`）が
`if ( … )` の閉じ括弧として解釈されるため。同じ書き方が7つのバッチファイルに
あったのでまとめて直した（`C:\Program Files (x86)` 配下に置いた場合も同じ症状）。

**原因の切り分けに使う道具が動かないと、原因の切り分けができない。**
利用者からの「何も出ずにウィンドウが消える」という報告で分かった。


### 4.13 実データで見つかった体裁を、合成サンプルに足した

4.11・4.12 で直した不具合は、いずれも**合成サンプル16枚には無い体裁**だった。
そのため数値には表れず、**実名刺を見るまで気づけなかった**。同じことを繰り返さ
ないよう、その体裁をレイアウトとして追加した（`spread`、20枚へ）。

- 姓と名を大きく離し、ふりがなをそれぞれの上に置く（離れた分が別の行として読まれる）
- ロゴの丸とQRコード（`©` `回回` のような文字として読まれる）
- `T E L :` のように字間を空けたラベル

追加した直後に、**4.12 の修正の弱点が1件見つかった。** ロゴの読み崩れ
（`トイ ヽ っ`）から2文字の断片 `トイ` を氏名として拾っていた。行そのままで
氏名を探しきってから断片を見る、断片は3文字以上に限る、と直した。
**体裁を1つ足しただけで、入れたばかりの規則の穴が出た。**

サンプルが16枚→20枚に増えたため、§3の数値は前回までと直接は比較できない。
新しいレイアウトは難しめで、同じ実装でも総合は下がる（当時の数値。16枚 63.8% ↔ 20枚 66.8%
は別の母集団の数字）。**以後の比較は20枚を基準とする。**

> 縦書き言語データ（`jpn_vert`）の効果も測り直した。16枚では +0.5ポイントだったが、
> 20枚では **C 68.0% ↔ D 68.0%** で並ぶ。縦書き名刺そのものの
> 修正項目数（10.5項目/枚）は変わらないため、**採用は維持**する
> （縦書きが読めなくなる害のほうが大きい）。処理時間は +1.0秒/枚。

### 4.14 左右2段組みの名刺で、段が1行に混ざっていた

実テスト（225枚）の3枚で、項目がほぼ全滅していた。**原因はOCRではなく、
行の扱いだった。**

名刺は左に社名・ロゴ、右に連絡先を置く体裁が多い。OCRは同じ高さにある文字を
1行にまとめて返すため、左段の飾り・ロゴと右段の本文が1行に混ざる。

| OCRが返した1行 | 実際は |
| --- | --- |
| `de Fh SHB eA        宮田 修` | ロゴの読み崩れ ＋ 氏名 |
| `A          とみた     おさむ` | 飾り ＋ ふりがな（姓・名） |
| `Sangeon Lee    T +82.2.6421.7777` | 氏名 ＋ 電話 |
| `mobile: 090-…       る 。` | 携帯 ＋ 裏写りの読み崩れ |

段の間は必ず広く空くので、**空白2つ以上を段の区切り**として分ける。語の間の
空白1つ（`沖縄県 東京事務所` `T E L:03-…` `佐々木 健`）は区切りにしない。

分けたことで見つかった問題を4件あわせて直した。

| 件 | 症状 | 対応 |
| --- | --- | --- |
| 頭文字1文字の連絡先ラベル（`T` `F` `C`） | 3件とも電話として扱われ、先に入った1件だけが残る（FAX・携帯が空） | 前が英数字でなく直後に番号が来る場合に限りラベルとして読む |
| 国別ドメイン | `eonlee@…co.kr` の `.kr` を知らず社名『NEXON GAMES』と照合できない | 2文字の末尾を外す。ドメインが社名を縮めた場合（`edgecre` ← `Edge Creators`）も先頭一致で見る |
| 離れて読まれたふりがな | `とみた` `おさむ` を1行と見て せい『とみ』めい『た』に割る | 続く行もひらがななら1組として扱う（間にロゴ・部署が挟まってもよい） |
| 英字だけの行を氏名にする | 社名を氏名として登録（姓『Edge』名『Creators』）、本来の『坂本』が空 | 日本語の氏名を先に採る。日本語の候補が無いときだけ英字を採る（英語の名刺は変わらない） |

**7枚目（韓国の名刺）の変化**

| 項目 | 修正前 | 修正後 |
| --- | --- | --- |
| 氏名 | （空） | Sangeon Lee |
| 会社名 | （空） | NEXON GAMES |
| 部署 | （空） | Planning & Coordination Dept |
| 電話 / FAX / 携帯 | 1件だけ | 3件すべて |
| 住所 | `GAMES   2621, Nambusunhwan-ro,`（段が混ざったまま） | — |

**合成サンプル20枚での再計測：65.3% → 66.8%**（修正項目 4.5 → 4.3項目/枚。この修正だけの効果で、最新の数値は[§3](#3-計測結果)の表）。
項目ごとの差分は4件すべて改善で、悪くなった項目は無い。

> **残る誤りはOCRの誤読**で、ここでは直せない。同じ3枚で
> `冨田`→`宮田`、`企業誘致`→`企業請致`、`匡志`→`EBS` と読まれている。
> 段の分割で「取り出し方の問題」と「読み取りの問題」が分離できたことで、
> 次に手を入れるべきなのが**読み取り側**であることが確定した。

---

## 5. 残っている弱点と、次に測るべきこと

| 弱点 | 現状 | 次の検証 |
| --- | --- | --- |
| 縦書き名刺 | 修正10.0項目/枚。実質的に手入力に近い | クラウドOCR・LLM抽出で再計測。改善しなければ「縦書きは手入力」と運用で割り切る判断も要る |
| ふりがな | せい・めい とも50%（0%・6%から改善）。残りは**OCRが文字を落としている**もの（`すず き いち ろう` の `すず` が消える等）で、分割では救えない | LLM抽出でも救えない。読み取りの精度そのものが要る（クラウドOCR・PaddleOCR） |
| URL | 50%。ドットの後の空白は詰めたが、まだ半分は読み取り自体が崩れている | LLM抽出で補正できる可能性 |
| 装飾の多い名刺 | 修正2.5項目/枚 | 背景色・ロゴが認識を妨げている。クラウドOCRの領域検出に期待 |
| 住所 | 85% | 残りは番地の数字・ハイフンの誤認識が中心 |

### 5.1 海外名刺への対応（言語データと項目分離は別問題）

韓国語・中国語・ロシア語の言語データを入れて実測した。

| 構成 | 日本語名刺の正答率 | 処理時間/枚 |
| --- | ---: | ---: |
| 現行 `jpn+jpn_vert+eng` | 52.9% | 3.01 秒 |
| ＋韓国語＋中国語＋ロシア語 | 53.8% | 4.32 秒 |

（絶対値は 4.8 の修正前の測定。言語データによる差分として読む）

**精度は落ちない**（当初は劣化を見込んでいたが外れた）。ただし**処理時間が44%増える**。

より重要なのは、**言語データを入れても項目が埋まらない**こと。

| 名刺 | 取れた項目（7項目中） | 落ちたもの |
| --- | ---: | --- |
| 英語 | 6 | 電話（4.6で対応済み） |
| 中国語 | 4 | 会社名・役職 |
| 韓国語 | 3 | 会社名・役職・氏名 |
| ロシア語 | 1 | ほぼ全部 |

法人格の辞書が `株式会社` / `Inc.` しか持たないためで、`주식회사`・`有限公司`・`ООО`
は未登録。**言語データの追加と、項目分離の対応は別作業**であり、後者が本体。

したがって全言語を常時読み込むのではなく、`BCARDS_OCR_LANGUAGES` で
**必要なときだけ足せる**現在の形を維持する。辞書の拡充は、実データで
どの言語の名刺が何枚あるかを数えてから優先順位を決める。

### 5.2 別のOCRエンジンを実測した（EasyOCR / PaddleOCR）

比較表を眺めるのではなく、**同じサンプル・同じ前処理・同じ項目分離**で
読み取りエンジンだけを差し替えて測った（構成F・G）。

| 構成 | エンジン | 項目正答率 | 修正項目/枚 | 処理時間/枚 |
| --- | --- | ---: | ---: | ---: |
| D | tesseract（現行） | **68.0%** | **4.2** | **4.7 秒** |
| G | EasyOCR | 64.1% | 4.7 | 16.8 秒 |
| F | PaddleOCR | 未計測 | — | — |

> 構成Fは、計測環境からモデルの配布元（HuggingFace / ModelScope / BOS）へ
> 到達できないため未計測。アダプタは動く状態で入れてあるので、社内の端末では
> `pip install -r requirements-paddle.txt` → `python -m poc.runner --only F` で測れる。

**総合ではtesseractが上だが、項目ごとに見ると壊れ方が逆だった。**

| 項目 | tesseract | EasyOCR |
| --- | ---: | ---: |
| せい / めい | 50% | **75%** |
| 会社名 | 69% | **75%** |
| 郵便番号 | 69% | **88%** |
| 電話 | 75% | **94%** |
| 住所 | **81%** | 38% |
| 部署 | **62%** | 38% |
| 役職 | **62%** | 44% |
| FAX | **91%** | 64% |
| メール | **62%** | 25% |
| URL | **50%** | 0% |

同じ名刺の読みを並べると理由が分かる。

| 印字 | tesseract | EasyOCR |
| --- | --- | --- |
| `株式会社サンプル商事` | `株 式 会 社 サ ンプ ル 商 事` | `株式会社サンプル商事` |
| `やまだ たろう` | `や まだ た ろう` | `やまだ たろう` |
| `東京都千代田区千代田1-1-1` | `東京 都 千 代田 区 千代 田 1-1-1` | `東京都千代田区千代田1-1-1` |
| `taro.yamada@example.co.jp` | `taro, yamada@example.co, jp` | `taro.yamada@exampLe.co.jp` |
| `https://www.example.co.jp` | `https://www. jp` | `https:l_ 'WWW. exampLe.co.jp` |

- **EasyOCRは日本語をきれいに読む。** 語の途中に空白を入れないため、
  4.10 で直したような分割の細工が要らない（せい・めいが 50% → 75%）
- **EasyOCRは英数字を崩す。** `l` を `L`、`//` を `l_` と読むためメール・URLが壊れる
- **tesseractはその逆**で、英数字は読めるが日本語に空白を入れる

**縦書き名刺はどちらも苦手**（修正 10.0 → 11.0 項目/枚）で、期待した改善は無かった。

> **最初の集計では住所を 31%、メールを 12% としていたが、一部はこちらの
> 不具合だった。** 領域を行にまとめる幅を名刺全体の間隔から決めていたため、
> 行間の狭い名刺で郵便番号の行と役職の行を1行に寄せ、郵便番号を抜いた残り
> （`代表社員`）を住所として登録していた（16枚中3枚）。その領域の文字の高さで
> 決めるように直し、住所 31% → 38%、郵便番号 81% → 88%、総合 55.2% → 56.2%。
> **エンジンの優劣を測るには、まず自分の側の不具合を潰す必要がある。**

> **提案：項目ごとに使うエンジンを分ける。** 日本語が主の項目（ふりがな・会社名・
> 郵便番号・電話）をEasyOCR、英数字が主の項目（メール・URL・住所・部署・役職）を
> tesseract から採ると、同じ20枚で **68.0% → 74.1%**（+6.1ポイント）になる。
> 259項目のうち176→192項目が一致する計算。
>
> ただしこれは**同じ20枚を見て使い分けを決めた数字**なので、上限とみるべき。
> 実名刺で測り直すまで、この差はそのままには受け取れない。
>
> **この差は縮んでいる。** 初回の計測では +8.1ポイント（65.3% → 73.4%）だったが、
> 抽出側（左右2段組みの分割・ふりがなの組・連絡先ラベル）を直すたびに
> tesseract 側が上がり、使い分けの利得はそのぶん減る。**エンジンを増やす前に
> 自分の側で取れる分を取り切るほうが安い**（処理時間が約17秒/枚に増えないため）。
>
> ただし**2つのエンジンを両方動かすため処理時間は約17秒/枚**になる。
> 年3,000枚・同時取込2名の想定では許容できるが、実名刺で同じ傾向が出るかを
> 確かめてから実装したい（合成サンプル16枚の結果であり、実データではない）。

再現方法：

```bash
cd app
pip install -r requirements-easyocr.txt      # または requirements-paddle.txt
./.venv/bin/python -m poc.runner --only D,F,G --out /tmp/engines.md
```

### 5.3 LLM抽出（方式2）の計測手順

実装は完了している（`app/src/bcards/services/ocr/llm_extractor.py`）。
Claude API に名刺画像とOCRテキストの両方を渡し、JSON Schema による構造化出力で項目を受け取る。

```bash
export ANTHROPIC_API_KEY=...        # または ant auth login
cd app

# 構成Eだけを計測する（既存の構成A〜Dの再計測を省いて費用と時間を抑える）
./.venv/bin/python -m poc.runner --only E --out ../ocr-poc-report-llm.md

# 全構成を並べて比較する場合
./.venv/bin/python -m poc.runner --out ../ocr-poc-report.md

# エフォートを振って精度と費用の関係を見る場合
./.venv/bin/python -m poc.runner --only E --llm-effort medium --out /tmp/e-medium.md
```

計測すると、構成Eの行に**正答率・処理時間・1枚あたりの費用（トークン数から算出）**が入る。
モデルは既定で `claude-opus-5`、エフォートは `low`（抽出は単純なタスクのため）。
`--llm-model` / `--llm-effort`、または `BCARDS_LLM_MODEL` / `BCARDS_LLM_EFFORT` で変更できる。

アダプタ自体は認証情報が無くても検証済みで、`app/tests/test_llm_extractor.py` が
HTTPトランスポートを差し替えて次を確認している。

- 名刺画像（base64 JPEG）とOCRテキストの両方を送っていること
- 構造化出力のスキーマに全項目が含まれ、余分なキーを許さないこと
- 長辺1600pxを超える画像は縮小してから送ること（費用と転送量の抑制）
- 拒否応答を抽出結果として扱わないこと
- `auto` ではLLMが失敗してもルールベースへ退避し、取込が止まらないこと

したがって鍵を設定すれば、追加の実装なしにそのまま計測に入れる。

### 5.4 クラウドOCRの計測手順

Azure AI Document Intelligence のアダプタは実装済み（`BCARDS_OCR_PROVIDER=azure`）。
Google Document AI / Amazon Textract を比較する場合は
`services/ocr/providers.py` に同じインターフェースでクラスを追加する（30行程度）。

---

## 6. 論点Cへの回答（暫定）

| 論点Cの問い | 現時点の回答 |
| --- | --- |
| 方式1（ルールベース）で足りるか | **足りない。** 合成サンプルで項目正答率68.0%、1枚あたり4.2項目の修正が必要。ただし当初の34.3%から改善しており、**残る誤りの多くは読み取りそのものの限界**に移った |
| 方式2（LLM抽出）を採るべきか | **有力だが、当初考えたほど万能ではない。** ふりがなは分割規則の修正だけで 0% → 50% になった（[4.10](#410-ふりがなを先頭の空白で切っていた)）。LLMが効くのは役職・部署のような意味の判断が要る項目で、**文字が読めていない箇所は埋まらない** |
| どのOCRサービスにするか | **まだ決められないが、比較は始めた。** tesseract 68.0% / EasyOCR 64.1% を実測（[5.2](#52-別のocrエンジンを実測した-easyocr--paddleocr)）。**壊れ方が逆**で、項目ごとに使い分けると 74.1% の見込み（抽出側を直すたびに差は縮んでいる）。確定には実名刺が要る |
| 決定に必要なもの | ①実名刺サンプルと正解ラベル ②Claude API の鍵 ③比較するクラウドOCRの契約（試用可） |

**決裁をお願いしたいこと**：上記①〜③の手配。揃えば本レポートと同じ形式で数値を更新し、
論点Cを確定できる状態になる。

---

## 付録：再現方法

```bash
cd business-card-system/app
./run.sh                                    # 依存の導入（初回のみ）
apt-get install -y tesseract-ocr tesseract-ocr-jpn tesseract-ocr-jpn-vert

# 合成サンプルで計測（16枚・約5分）
./.venv/bin/python -m poc.runner \
    --out ../ocr-poc-report.md --json poc/last-result.json

# サンプル画像を目視確認したい場合
./.venv/bin/python -m poc.runner --save-samples /tmp/samples --out /tmp/r.md
```

生データ（1枚ごとの抽出結果と正解の対比）は `app/poc/last-result.json` に残る。
どの項目がどう誤ったかを1件ずつ確認できる。
